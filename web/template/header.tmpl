<!DOCTYPE html>

<!-- header.tmpl -->
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	{{if .meta.title}}<meta name="og:title" content="{{.meta.title |noescape}}">
	{{else}}<meta name="og:title" content="{{.instance.Title}} - GoToSocial">
	{{end}}
	{{if .meta.description}}<meta name="og:description" content="{{.meta.description |noescape}}">{{end}}
	<link rel="stylesheet" href="/assets/dist/_colors.css">
	<link rel="stylesheet" href="/assets/dist/base.css">
	{{range .stylesheets}}<link rel="stylesheet" href="{{.}}">
	{{end}}
	<link rel="shortcut icon" href="/assets/logo.png" type="image/png">
	<title>{{.instance.Title}} - GoToSocial</title>
</head>
<body>
	<div class="page">
		<header>
			<a aria-label="instance homepage" href="/" class="nounderline header">
				<img src="/assets/logo.png" alt="Instance Logo"/>
				<div>
					<h1>
						{{.instance.Title}}
					</h1>
				</div>
			</a>
		</header>
		<div class="content">
